# langchain-chatbot
A custom AI chatbot built using Langchain, Qdrant, and Flask. It extracts course data from Brainlox, generates embeddings, and stores them in a vector database for efficient searching. The Flask API handles user queries and retrieves the most relevant results. 🚀
